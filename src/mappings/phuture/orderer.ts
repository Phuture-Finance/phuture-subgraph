import { PlaceOrder, UpdateOrder } from '../../types/templates/Orderer/Orderer'

export function handlePlaceOrder(event: PlaceOrder): void {
  return;
}

export function handleUpdateOrder(event: UpdateOrder): void {
  return;
}
