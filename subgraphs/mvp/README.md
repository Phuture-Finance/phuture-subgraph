# MVP Subgraph

```bash
# generate config
npm run config
# run pre codegen
npm run precodegen
# run codegen
npm run codegen
# run post codegen
npm run postcodegen
# local deploy
npm run create:local && npm run deploy:local:mainnet
# remote deploy
npm run deploy:hosted:mainnet
```
